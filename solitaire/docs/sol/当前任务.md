# 当前任务入口

当前没有已发布给执行者的活动施工任务。唯一进度状态以 `docs/sol/项目状态.md` 为准。

负责人下一步：亲自把 `docs/sol/worker-order-s1.md` 的完整内容发布给执行者。技术总监不会代发、启动、恢复或陪跑执行者。

发布后执行者必须：

1. 第一条消息第一句严格写 `老大！天下第一！`。
2. 完整阅读合同 2.0 和全部 SOL 状态文件。
3. 连接实时 Godot Editor 的 Godot AI MCP，先证明项目路径、editor state 和 scene hierarchy 正确。
4. 只施工 WP-01 并提交 WP 证据包；在技术总监确认前不进入依赖 WP。
5. Cocos 仓库只读，不 commit/push/发布，不进入下一阶段。

如 MCP 未连接或必要能力不可用，执行者只提交最小负责人操作请求，不得降级为 CLI-only。
